# Development override - use with: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build
# This file adds build configuration for local development

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
